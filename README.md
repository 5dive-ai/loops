# 5dive-ai/loops

Agentic Loops registry — one-click-installable recurring agent workflows for the dashboard marketplace (DIVE-761). `index.json` holds the top-level `loops` array consumed by the dashboard. Schema is dev-owned; gated by Marcus.
